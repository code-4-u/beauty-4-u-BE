<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beauty4u.backend.goods.query.mapper.ReviewQueryMapper">
    <select id="findAllReview" resultType="com.beauty4u.backend.goods.query.dto.ReviewQueryDTO">
        SELECT
            r.review_created_date,
            r.review_id,
            g.goods_name AS goodsName,
            r.review_score,
            r.review_content
        FROM review r
        JOIN goods g ON (r.goods_code = g.goods_code)
        ORDER BY r.review_created_date DESC;
    </select>
</mapper>