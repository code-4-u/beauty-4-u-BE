<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beauty4u.backend.goods.query.mapper.ReviewQueryMapper">

    <select id="findAllReview" resultType="ReviewQueryDTO">
        SELECT
        r.created_date,
        r.review_id,
        g.goods_name AS goodsName,
        r.review_score,
        r.review_content
        FROM review r
        JOIN goods g ON (r.goods_code = g.goods_code)
        ORDER BY r.created_date DESC
    </select>

    <select id="findAllReviewSort" resultType="ReviewQueryDTO">
        SELECT
        r.created_date,
        r.review_id,
        g.goods_name AS goodsName,
        r.review_score,
        r.review_content
        FROM review r
        JOIN goods g ON (r.goods_code = g.goods_code)
        ORDER BY
        <choose>
            <when test="primarySort == 'review_score'">
                r.review_score
            </when>
            <otherwise>
                r.created_date
            </otherwise>
        </choose>
        <choose>
            <when test="primaryOrder == 'ASC'">
                ASC
            </when>
            <otherwise>
                DESC
            </otherwise>
        </choose>
<!--        두번째 정렬 조건 null 아닐 때만 추가 -->
        <if test="secondarySort != 'null' and secondarySort != ''">
            ,
            <choose>
                <when test="secondarySort == 'review_score'">
                    r.review_score
                </when>
                <when test="secondarySort == 'created_date'">
                    r.created_date
                </when>
            </choose>
            <if test="secondaryOrder != 'null' and secondaryOrder != ''">
                <choose>
                    <when test="secondaryOrder == 'ASC'">
                        ASC
                    </when>
                    <otherwise>
                        DESC
                    </otherwise>
                </choose>
            </if>
        </if>
    </select>
</mapper>