<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beauty4u.backend.user.query.mapper.RoleQueryMapper">

    <select id="findRoleList" resultType="RoleResDTO">
        SELECT
            user_role_id,
            user_role_name
        FROM user_role
    </select>
</mapper>