<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--  파일 최대 용량 -->
    <property name="MAX_FILE_SIZE" value="50MB"/>

    <!--  파일 최대 저장일자 -->
    <property name="MAX_HISTORY" value="7"/>

    <!--  파일명 -->
    <property name="API_LOG_PATH_NAME" value="c:/beautylog/goods-api.log"/>

    <!--  API -->
    <appender name="api" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${API_LOG_PATH_NAME}</file>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <provider class="net.logstash.logback.composite.loggingevent.ArgumentsJsonProvider"/>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${API_LOG_PATH_NAME}_%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- api package -->
    <logger name="com.beauty4u.backend.goods" level="INFO" additivity="false">
        <appender-ref ref="api"/>
    </logger>
    <!-- 콘솔 로깅 -->
<!--    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">-->
<!--        <encoder>-->
<!--            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>-->
<!--        </encoder>-->
<!--    </appender>-->

    <!-- Logstash 전송 시 사용할 TCP 통신 정보 -->
    <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>localhost:5000</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"application":"beauty4u"}</customFields>
        </encoder>
    </appender>

    <root level="INFO">
<!--        <appender-ref ref="CONSOLE"/>-->
        <appender-ref ref="LOGSTASH"/>
        <appender-ref ref="api"/>
    </root>

    <!-- 특정 패키지의 로그 레벨 조정 -->
    <logger name="com.beauty4u.backend" level="DEBUG"/>
</configuration>